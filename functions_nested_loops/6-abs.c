#include <stdio.h>
#include "main.h"

/**
* _abs - Print value of an integer
* @n: print value
*
* Return: value of an integer
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
